/* eslint-disable no-shadow */
export enum WAGMI_ACCOUNT_STATUS {
  CONNECTED = 'connected',
  CONNECTING = 'connecting',
  DISCONNECTED = 'disconnected',
  RECONNECTING = 'reconnecting'
}

export enum DATE_STATUS {
  BEFORE = 'before',
  DURING = 'during',
  AFTER = 'after'
}

export enum CURRENCY_ENUM {
  USDT = 'USDT'
}

export enum UNICODE_CHARS {
  horizontalEllipsis = '\u2026',
  enDash = '\u2013',
}
